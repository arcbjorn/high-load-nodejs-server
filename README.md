## Node.js server with load balancing using cluster

### Development

```bash
# install dependencies
yarn
# run once - to create dist folder
yarn build
# start the cluster
yarn serve
# test the load balancer using autocannon
yarn load-test
```
